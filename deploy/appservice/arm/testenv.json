{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentTemplate.json#",
  "contentVersion": "1.0.0.0",
  "metadata": {
    "_generator": {
      "name": "bicep",
      "version": "0.44.1.10279",
      "templateHash": "4922949895688901763"
    }
  },
  "parameters": {
    "prefix": {
      "type": "string",
      "defaultValue": "orchextest",
      "metadata": {
        "description": "Prefix for resource names."
      }
    },
    "yourUserObjectId": {
      "type": "string",
      "defaultValue": "",
      "metadata": {
        "description": "Your own user object id, from Entra ID > Users > your account > Object ID. It grants you access to\nadd the registry credentials to the vault. Leaving it empty deploys, but then nobody can.\n"
      }
    }
  },
  "variables": {
    "location": "[resourceGroup().location]",
    "suffix": "[uniqueString(resourceGroup().id)]",
    "storageName": "[toLower(take(replace(format('{0}{1}', parameters('prefix'), variables('suffix')), '-', ''), 24))]",
    "vaultName": "[take(format('{0}-{1}', parameters('prefix'), variables('suffix')), 24)]"
  },
  "resources": [
    {
      "type": "Microsoft.Storage/storageAccounts",
      "apiVersion": "2023-01-01",
      "name": "[variables('storageName')]",
      "location": "[variables('location')]",
      "sku": {
        "name": "Standard_LRS"
      },
      "kind": "StorageV2",
      "properties": {
        "minimumTlsVersion": "TLS1_2",
        "supportsHttpsTrafficOnly": true,
        "allowBlobPublicAccess": false
      }
    },
    {
      "type": "Microsoft.KeyVault/vaults",
      "apiVersion": "2023-07-01",
      "name": "[variables('vaultName')]",
      "location": "[variables('location')]",
      "properties": {
        "tenantId": "[subscription().tenantId]",
        "sku": {
          "family": "A",
          "name": "standard"
        },
        "enableRbacAuthorization": false,
        "enableSoftDelete": true,
        "softDeleteRetentionInDays": 7,
        "accessPolicies": "[if(empty(parameters('yourUserObjectId')), createArray(), createArray(createObject('tenantId', subscription().tenantId, 'objectId', parameters('yourUserObjectId'), 'permissions', createObject('secrets', createArray('get', 'list', 'set', 'delete')))))]"
      }
    }
  ],
  "outputs": {
    "storageAccountName": {
      "type": "string",
      "value": "[variables('storageName')]"
    },
    "keyVaultName": {
      "type": "string",
      "value": "[variables('vaultName')]"
    },
    "connectionStringHint": {
      "type": "string",
      "value": "[format('az storage account show-connection-string -n {0} -g {1} --query connectionString -o tsv', variables('storageName'), resourceGroup().name)]"
    }
  }
}